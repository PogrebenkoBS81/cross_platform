package pogrebenko.lab3db.sqldatabase.database;

/**
 * 'database' package contains various implementations of different DBs for app models.
 * <p>
 * If any new DB, like PostgreSQL needs to supported, then:
 * 1) Create new postgresql package.
 * 2) Create required model packages inside it (like 'medicine').
 * 3) By analogy with already implemented DBs implement it
 * <p>
 * All implementation should be simple, just do CTRL+C CTRL+V from any implemented package,
 * and write new SQL queries for your new SQL DB.
 */
@SuppressWarnings("unused")
public class Overview {
}
